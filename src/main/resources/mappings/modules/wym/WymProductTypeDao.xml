<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.wym.dao.WymProductTypeDao">
    
	<sql id="wymProductTypeColumns">
		a.id AS "id",
		a.shop_id AS "shopId",
		a.vip_id AS "vipId",
		a.app_type AS "appType",
		a.name AS "name",
		a.remark AS "remark",
		a.parent_id AS "parent.id",
		a.parent_name AS "parentName",
		a.parent_ids AS "parentIds",
		a.level_no AS "levelNo",
		a.sort AS "sort",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="wymProductTypeJoins">
	</sql>
    
	<select id="get" resultType="WymProductType">
		SELECT 
			<include refid="wymProductTypeColumns"/>
		FROM wym_product_type a
		<include refid="wymProductTypeJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="WymProductType">
		SELECT 
			<include refid="wymProductTypeColumns"/>
		FROM wym_product_type a
		<include refid="wymProductTypeJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="parent != null and parent.id != null and parent.id != ''">
				AND a.parent_id = #{parent.id}
			</if>
			<if test="parentIds != null and parentIds != ''">
				AND a.parent_ids LIKE 
					<if test="dbName == 'oracle'">'%'||#{parentIds}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{parentIds}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{parentIds},'%')</if>
			</if>
		</where>
		ORDER BY a.sort ASC
	</select>
	
	<select id="findAllList" resultType="WymProductType">
		SELECT 
			<include refid="wymProductTypeColumns"/>
		FROM wym_product_type a
		<include refid="wymProductTypeJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		ORDER BY a.sort ASC
	</select>
	
	<select id="findByParentIdsLike" resultType="WymProductType">
		SELECT
			a.id,
			a.parent_id AS "parent.id",
			a.parent_ids
		FROM wym_product_type a
		<include refid="wymProductTypeJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			AND a.parent_ids LIKE #{parentIds}
		</where>
		ORDER BY a.sort ASC
	</select>
	
	<insert id="insert">
		INSERT INTO wym_product_type(
			id,
			name,
			remark,
			parent_id,
			parent_ids,
			sort,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{name},
			#{remark},
			#{parent.id},
			#{parentIds},
			#{sort},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE wym_product_type SET 	
			name = #{name},
			remark = #{remark},
			parent_id = #{parent.id},
			parent_ids = #{parentIds},
			sort = #{sort},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="updateParentIds">
		UPDATE wym_product_type SET 
			parent_id = #{parent.id}, 
			parent_ids = #{parentIds}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE wym_product_type SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id} OR parent_ids LIKE '%,'||#{id}||',%'
	</update>
	
</mapper>